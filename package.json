{
  "name": "short-uri-project",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @shorturi/web",
    "build": "rm -rf functions && npm run build --workspaces --if-present",
    "deploy": "npm run build && echo 'Deploy to Cloudflare Pages via Git integration'"
  },
  "devDependencies": {
    "@types/node": "^20.19.10",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": "^20 || ^22"
  }
}
